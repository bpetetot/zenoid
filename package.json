{
  "name": "zenoid",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "workspaces": ["packages/*"],
  "private": true,
  "scripts": {
    "build:core": "yarn workspace @zenoid/core build",
    "prebuild:cli": "yarn build:core",
    "build:cli": "yarn workspace @zenoid/cli build",
    "start:cli": "yarn workspace @zenoid/cli start"
  }
}
